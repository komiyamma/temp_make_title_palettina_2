# 作業記録: タイトル生成

`list.txt` に記載された15件のIDについて、以下の作業を完了しました。

1. `tags/` および `critique/` フォルダのメタデータから内容を解釈。
2. 当該内容に基づき、各画像に対する適切な英語・日本語のタイトルを生成。
3. 生成したタイトルを `title/` フォルダに `<ID>.en.txt` および `<ID>.ja.txt` として保存。

※ 本リポジトリには自動テストが存在しないため、テスト実行はスキップし、手動でファイルの生成状況を確認しました。
